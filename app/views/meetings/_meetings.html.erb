<% 10.times do |i| %>
  <div class="timespan" id="">
    <div class="span">
      <h3><%=  local_date(Time.now + i.day, '%b %e') %></h3>
    </div>
    <%= render partial: 'meetings/meeting', locals: { i: i } %>
  </div>
<% end %>
